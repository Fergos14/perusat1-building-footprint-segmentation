# Interactive Maps

This directory stores the Folium HTML maps generated by `embeddings.ipynb`.

The notebook writes:

- `kmeans_clusters.html`
- `acquired_image_footprints_by_cluster.html`

These files are kept outside the notebook so the notebook remains lightweight for GitHub.
